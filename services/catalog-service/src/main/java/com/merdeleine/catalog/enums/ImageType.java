package com.merdeleine.catalog.enums;

public enum ImageType {
    ORIGINAL,   // 原始上傳圖
    THUMBNAIL,  // 200px — 列表縮圖
    GALLERY,    // 400px — 商品頁
    DETAIL,     // 800px — zoom
    MAIN        // 保留舊有（可供其他用途）
}
